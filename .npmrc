# pnpm resolves peers strictly by default, which tends to hard-fail installs
# on transitive peer mismatches (e.g. eslint plugins) rather than warning.
# Relax that for this workspace; auto-install-peers keeps peer deps present
# without hand-declaring each one in every package.
auto-install-peers=true
strict-peer-dependencies=false
